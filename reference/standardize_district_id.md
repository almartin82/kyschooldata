# Standardize district ID to 3 digits

Kentucky district IDs are 3 digits (001-999).

## Usage

``` r
standardize_district_id(x)
```

## Arguments

- x:

  District ID vector

## Value

Character vector with zero-padded 3-digit IDs
