# Standardize school ID to 6 digits

Kentucky school IDs are typically 3-digit district + 3-digit school.

## Usage

``` r
standardize_school_id(x)
```

## Arguments

- x:

  School ID vector

## Value

Character vector with zero-padded 6-digit IDs
